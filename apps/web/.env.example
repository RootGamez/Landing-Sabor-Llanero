# URL base del Worker de la API (apps/api). En local, `wrangler dev` sirve en
# el puerto 8787 por defecto. En producción, apuntar al Worker desplegado
# (ver apps/api/wrangler.toml → [env.production] name).
NEXT_PUBLIC_API_BASE_URL=http://localhost:8787
